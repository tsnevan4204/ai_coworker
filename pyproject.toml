[project]
name = "yaco"
version = "0.1.0"
description = "Your AI coworker at every meeting"
requires-python = ">=3.11,<3.12"
readme = "README.md"

[tool.ruff]
line-length = 100
target-version = "py311"
